defmodule Onicn.Foods.ColdWheatSeed do
  use Onicn.Categories.Food

  section "简介" do
    "冰霜小麦每次成年时可以收获18份冰霜麦粒。冰霜麦粒不能直接食用，但可以进一步加工成冰霜面包、火椒面包或者浆果糕。"
    "冰霜小麦每18周期收获18单位冰霜麦粒，即平均每周期收获1单位冰霜麦粒。"
  end
end
