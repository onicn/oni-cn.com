defmodule Onicn.Elements.DirtyIce do
  use Onicn.Categories.Solid,
    cn_name: "污染冰"

  section "简介" do
    "污染冰是一种水资源，大量存在于冰冻区内。"
  end

  section "用途" do
    "在冰冻区的建筑散发的热量会将污染冰自然融化成污染水。"
  end
end
