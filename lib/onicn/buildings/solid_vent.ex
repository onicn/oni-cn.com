defmodule Onicn.Buildings.SolidVent do
  use Onicn.Categories.Building

  section "简介" do
    "轨道滑槽能够将自运输轨道输入的固体扔到世界中。"
  end

  section "用途" do
    "与运输存放器相比，不会因运输存放器装满而阻塞轨道。"
    "建议离地面起码一格建造，否则外面的东西可能会堵塞出口。"
  end
end
