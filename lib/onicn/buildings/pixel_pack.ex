defmodule Onicn.Buildings.PixelPack do
  use Onicn.Categories.Building

  section "简介" do
    "像素屏是装饰性建筑，可以在两组不同的颜色间切换。利用信号线组可以分别控制四个颜色。"
  end

  section "小技巧" do
    "像素屏建造材料较便宜，不占用建筑空间，可以提供较高的装饰度，并且将管道挡在后面，让殖民地看起来更漂亮。"
  end
end
