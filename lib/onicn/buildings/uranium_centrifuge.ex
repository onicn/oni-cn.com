defmodule Onicn.Buildings.UraniumCentrifuge do
  use Onicn.Categories.Building

  section "简介" do
    "铀素离心机能从铀矿中以 20% 的效率提取浓缩铀，同时输出剩余 80% 质量的熔融铀。"
    "其内部可存储最多 30 千克的铀矿，每次处理 10 千克铀矿，生产的标准用时为 40 秒。"
  end
end
