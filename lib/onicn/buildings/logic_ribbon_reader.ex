defmodule Onicn.Buildings.LogicRibbonReader do
  use Onicn.Categories.Building
  section "简介" do
    "线组读取器从信号线组读取信号，可以指定四条信号中的一条来输出。"
  end
  section "小技巧" do
    "会在占用的格子之间导热，在热敏感环境使用要注意。"
  end
end
