defmodule Onicn.Buildings.ItemPedestal do
  use Onicn.Categories.Building

  section "简介" do
    "基座属于装饰性建筑，上面可以摆放一个物体，如果摆放的物体装饰度为正值，则会将装饰度加倍，如果为负值则增加一些装饰度。"
  end

  section "小技巧" do
    "火箭带回来的太空工艺品不能放到箱子里，只能放在基座上。"
  end
end
